// JavaScript wrapper for AgentList components
// Use the compatible version instead of TSX

const { AgentListCompat } = require('./AgentListCompat');

module.exports = {
  AgentList: AgentListCompat,
};